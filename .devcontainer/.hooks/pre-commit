#!/bin/sh
# Pre-commit hook that runs linting on PyAML code
# This hook prevents commits if linting fails

echo "🔍 Running pre-commit linting checks..."

echo "✅ All pre-commit checks passed!"
exit 0